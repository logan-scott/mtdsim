# Pinned runtime + dev dependencies for reproducible builds.
# Versions chosen for Python 3.11+ compatibility (tested on 3.13).
numpy==2.1.3
pandas==2.2.3
matplotlib==3.9.2
PyYAML==6.0.2
pytest==8.3.3
